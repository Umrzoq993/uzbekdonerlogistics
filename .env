# Unified environment file for both local development and server build
# Copy or symlink this file if you need environment specific variants.
# Only expose variables prefixed with VITE_ to the client bundle.

# -------------------- Backend API --------------------
# Base URL of the backend API (client + build time)
VITE_API_BASE_URL=https://api.uzbekdoner.uz

# Enable real /flials/active API endpoint (otherwise localStorage fallback)
VITE_ENABLE_FLIAL_ACTIVE_API=false

# -------------------- Dev Server Options --------------------
# Port for Vite dev server (server.port)
VITE_PORT=5173
# Enable polling file watcher (useful inside some Docker/VPS)
VITE_POLL=false

# Disable Hot Module Replacement entirely if true
DISABLE_HMR=false

# Optional additional allowed hosts (comma separated). Leave empty normally.
ALLOWED_HOSTS=

# Profile flag (used for conditional server behavior if extended later)
DEV_PROFILE=local

# Optimize apex charts pre-bundling (set to true only if you see performance issues)
VITE_APEX_OPTIMIZE=false

# -------------- Notes --------------
# For production build (npm run build) Vite will read this file.
# To override in production without committing secrets, create an external
# environment export (e.g. in your deployment pipeline) before running build.
# Example: DEV_PROFILE=server VITE_API_BASE_URL=https://api.example.com npm run build
