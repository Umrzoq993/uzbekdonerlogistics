############################################
# Single Environment File Example          #
############################################

# Profile: local | server
DEV_PROFILE=local

# Dev server port
VITE_PORT=5173

# Remote / Ngrok (enable if tunneling)
# HMR_PROTOCOL=wss
# HMR_HOST=xxxxx.ngrok-free.app
# HMR_CLIENT_PORT=443
# ALLOWED_HOSTS=xxxxx.ngrok-free.app

# Server profile example (uncomment + adjust)
# DEV_PROFILE=server
# HMR_PROTOCOL=wss
# HMR_HOST=logistika.adminsite.uz
# HMR_CLIENT_PORT=443
# ALLOWED_HOSTS=.adminsite.uz

# Optional: pre-optimize apex charts (default excluded in dev)
# VITE_APEX_OPTIMIZE=true

# Enable polling if FS events fail (Docker/VPS)
# VITE_POLL=1

# Optional API base (if needed in code)
# VITE_API_BASE=https://api.example.com

# Copy this file to .env and edit values. Keep secrets OUT of Vite unless prefixed with VITE_.
